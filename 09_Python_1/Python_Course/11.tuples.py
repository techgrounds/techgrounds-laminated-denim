#Tuples are immutable; they cannot be changed or modified.
#They are created with open/close round brackets.
coordinates = (4, 5)
#Items in a tuple can be accessed via index
print(coordinates[0])
#coordinates[1] = 10 would result in an error.
#